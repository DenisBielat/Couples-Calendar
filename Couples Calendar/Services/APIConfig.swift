import Foundation

/// API keys and configuration
/// In a production app, these would be stored in a more secure location
/// (e.g., Keychain, server-side proxy, or obfuscated config)
enum APIConfig {
    static let ticketmasterAPIKey = "tdV0n4EPBCaSRHguPIhu71AAz20SGAYB"
    static let ticketmasterBaseURL = "https://app.ticketmaster.com/discovery/v2"
}
